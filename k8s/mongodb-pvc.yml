kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mongodb-pvc
  namespace: chat-app
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests: 
      storage: 5Gi
  volumeName: mongodb-pv